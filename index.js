/**
 * @format
 */

import { AppRegistry } from 'react-native';
import KRTRMeshApp from './App';

// Register the app with the name 'main' which is what the iOS app expects
// This is specified in AppDelegate.swift: withModuleName: "main"
AppRegistry.registerComponent('main', () => KRTRMeshApp);
