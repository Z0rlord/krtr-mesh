name: Build Both Platforms

on:
  workflow_dispatch:

jobs:
  build_ios:
    name: Build iOS Preview
    type: build
    params:
      platform: ios
      profile: preview
      
  build_android:
    name: Build Android Preview
    type: build
    params:
      platform: android
      profile: preview
