/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// Register the app with the name 'main' which is what the iOS app expects
AppRegistry.registerComponent('main', () => App);
